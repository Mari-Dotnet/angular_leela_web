import { IsDogDirectiveDirective } from './is-dog-directive.directive';

describe('IsDogDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IsDogDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
