import { DynamiclistDirective } from './dynamiclist.directive';

describe('DynamiclistDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamiclistDirective();
    expect(directive).toBeTruthy();
  });
});
